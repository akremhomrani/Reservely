package tn.reservely.backend;

import org.junit.jupiter.api.Test;

class ReservelyBackendApplicationTests {

    @Test
    void contextLoads() {
        // Spring context smoke test — requires DB, so runs only in integration profile
    }
}
